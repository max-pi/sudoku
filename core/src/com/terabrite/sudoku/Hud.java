package com.terabrite.sudoku;

import com.badlogic.gdx.graphics.g2d.SpriteBatch;

public class Hud {

	// essentially the class that handles the rendering for the game? GameScreen will call this.
	// hmm, maybe should just have it in GamePlayScreen, cuz MenuScreen will just handle its own rendering

	public void render(SpriteBatch batch) {
		
		// render the sudoku grid
		
		// render the input numbers
		
		// render other buttons (check mistakes)
	
	
	
	}





}

// 9 buttons across the bottom of the screen to select the number
// error check button



